#include <stdio.h>
/**
 * main - main entry point
 * @argc: argument count
 * @argv: array of pointers of strings
 * Return: 0
 */
int main(int argc, char *argv[])
{
	(void)argc;

	printf("%s\n", argv[0]);
	return (0);
}
